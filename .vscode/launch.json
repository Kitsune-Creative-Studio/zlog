{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Zig Main",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/zig-out/bin/satisfactoryTools.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "zig-build"
        }
    ]
}
